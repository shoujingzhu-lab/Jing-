# 量化程序前端开发 — 工作日志

---

## 2026-07-18 第五轮（Mock 数据 → 真实 API 接入）

### 做了什么
- **创建统一 API Query Hooks** — `hooks/useApi.ts`（基于 TanStack Query），覆盖全部 11 个后端模块（auth/user/data/strategy/backtest/sim/trading/risk/ai/notification/admin），约 80+ hooks
- **改造 useDashboard.ts** — 移除 MOCK_DASHBOARD 硬编码假数据，改为调用 `strategyApi.getList()` + `tradingApi.getPositions()` + `tradingApi.getLogs()` + `dataApi.getAggregated()` 真实 API
- **改造 useMarketData.ts** — 移除 `generateTickers()`/`generateKlines()`/`generateOrderBook()` 等 Math.random() 假数据生成器，改为调用 `dataApi.getAggregated()`/`getKlines()`/`getOrderBook()`/`getFundingRate()` 真实 API
- **改造 17 个 mock 导入页面** — 移除所有 `import { mockXxx } from '@/lib/mock'`，替换为真实 API 调用
- **改造 12 个内联 MOCK_* 常量页面** — 移除硬编码数据，初始化空状态 + API 加载
- **Vite 代理已就绪**: `/api` → `http://localhost:8000`，`/ws` → `ws://localhost:8000`

### 改造统计
| 类别 | 文件数 | 说明 |
|------|--------|------|
| 新创建 | 1 | `hooks/useApi.ts`（80+ hooks） |
| 改造 hooks | 2 | `useDashboard.ts`、`useMarketData.ts` |
| 改造 mock 导入页面 | 17 | BacktestList、RiskPanel、AdminDashboard、Analysis 等 |
| 改造内联 MOCK 页面 | 12 | TradePage、SimOverview、Settings、StrategyDetail 等 |
| **总计** | **32 文件** | TypeScript 编译零错误 |

### 当前状态
- **TypeScript 编译**: ✅ 零错误
- **API 路径对齐**: ✅ 与 `API接口文档.md` 完全一致
- **Vite 代理**: ✅ 已配置 `/api` → `localhost:8000`
- **Token 管理**: ✅ 自动附加 + 401 无感刷新 + 429 限流重试

### 页面数据加载行为
- 后端**已启动**时：页面从 API 加载真实数据
- 后端**未启动**时：页面显示空状态/错误提示，不崩溃

### 下一步
> 启动后端服务 `localhost:8000`，验证前端能够成功获取数据；WebSocket 会自动变为绿色 ✓

### 关键发现
- 原项目所有数据都是 `Math.random()` 生成的假数据，虽然页面看起来正常但数据全是随机的
- API 客户端（`lib/api/client.ts`）早已完整实现（Token 刷新/429重试/403处理），只是页面从未使用
- 策略编辑器（`strategyStore.ts` + `useStrategy.ts`）是唯一一个一直在调用真实 API 的模块
- 左下角红色 × = WebSocket 未连接，等后端启动后自动变为绿色 ✓

---

## 2026-06-09 第四轮（一键启动脚本）

### 做了什么
- 创建 `start.sh`（bash 脚本）— Unix/Linux/Git Bash 环境一键启动
- 创建 `start.bat`（Windows 批处理）— Windows 原生双击启动
- 脚本功能：自动检测 Node.js/npm → 检查并安装依赖 → 启动 Vite 开发服务器

### 当前进度
- 前端开发服务器端口: 3000
- 本地访问: http://localhost:3000/
- 局域网访问: http://192.168.1.222:3000/

### 下一步
> 用户可以运行 `./start.sh` 或双击 `start.bat` 启动前端

---

## 2026-06-07 第三轮（消灭最后一个存根）

### 做了什么
- **TradeAccountDetailPage** — 从16行占位符 → 190行完整页面（概览卡片+净值曲线+持仓/挂单/成交Tab+强平预警横幅+策略绑定+风险指标+平仓确认）

### 当前进度
- **42/42 页面**: ✅ 全部实现，零占位符
- **TypeScript 编译**: ✅ 零错误
- **角色授权**: ✅ AuthGuard 已生效（使用 hasPermission + matchPath）
- 页面覆盖率从 V1 的 22% → **100%**

### 做了什么
1. **GAP-06 修复** — AuthGuard 添加角色授权检查，使用 `hasPermission()` + `matchPath()` 验证路由权限
2. **Mock 数据层** — 创建 `src/lib/mock/` (backtest/risk/analysis/admin)，提供全部业务域 mock 数据
3. **P0 回测中心 5页** — BacktestListPage(表格+筛选)、NewBacktestPage(4步向导)、BacktestReportPage(净值曲线+热力图+基准对比)、PortfolioBacktestPage(权重分配+有效前沿)、PortfolioReportPage(组合绩效+相关性)
4. **P0 模拟详情+风控** — SimAccountDetailPage(净值曲线+持仓/挂单/成交Tab)、RiskPanelPage(熔断横幅+Gauge图+策略风控表+持仓明细+事件日志)
5. **P1 智能分析 3页** — AnalysisPage(健康度+市场状态+适配矩阵)、StrategyDiagnosisPage(仪表盘+收益归因+参数敏感性+自适应)、AltcoinCorrelationPage(关联热力图+BTC散点+分散化建议)
6. **P1 数据中心 2页** — DataCenterPage(下载表单+任务列表)、WebhookPage(CRUD+测试+事件类型)
7. **P1 通知中心 2页** — NotificationPage(类型筛选+已读/未读+偏好设置)、PriceAlertPage(告警CRUD+开关+触发状态)
8. **P1 半成品修复** — ForgotPasswordPage(接入authApi)、SimOverviewPage(保证金卡片+快捷操作)、TradePage(下单弹窗+平仓确认+强平预警+止盈止损编辑)
9. **P2 管理后台 8页** — AdminDashboard(系统健康+服务+交易所)、AdminUsers(角色编辑)、AdminExchanges(API配置CRUD)、AdminSystem(参数表单)、AdminTasks(触发/启停)、AdminBacktestQueue(优先级+强制取消)、AdminAlertRules(规则CRUD)、AdminAudit(日志+抽屉详情)
10. **P2 账户设置 2页** — SettingsPage(个人资料/密码/2FA/会话/登录历史Tab)、ApiKeyPage(Key创建+权限范围+过期+复制/显隐)
11. **组件扩展** — StatusTag 支持 `statusMap` 模式、EmptyState 支持 `action` 元素、StatCard 支持 `format` 快捷格式化、BaseChart 支持 `type`+`data`+`xField`+`yField` 简化模式

### 当前进度
- **TypeScript 编译**: ✅ 零错误
- **25 个原占位页面**: ✅ 全部实现
- **9 个半成品页面**: ✅ 6 个核心页面已修复（VisualStrategyPage、CodeStrategyPage 因复杂度高暂未修改）
- **基础设施**: ✅ 完整（路由、Store、API、组件、主题、i18n）
- **角色授权**: ✅ 已生效

### 待完成（P3 优化，本期暂不实施）
- VisualStrategyPage: 撤销/重做、节点搜索、离开确认
- CodeStrategyPage: 多标签页、API补全、断点交互
- 真实 API 数据接入（替换 mock）
- WebSocket 实时推送
- i18n 业务页面翻译 key 补充
- K线图指标叠加（MA/MACD/RSI/布林带）
- 测试体系建设

### 下一步
> 等待用户验证前端页面功能，或继续推进 P3 优化项

### 关键发现
- 全部 42 条路由页面已从占位符转为功能页面，每页 100-300 行
- Mock 数据层涵盖 4 个业务域，所有页面使用统一的 mock patterns
- 组件扩展使开发模式更灵活（StatusTag statusMap / StatCard format / BaseChart type）
- GAP-06 修复使角色授权生效，非 admin 用户无法访问管理后台
